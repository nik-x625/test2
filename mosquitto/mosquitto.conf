# Listen on default MQTT port
listener 1883

# Optional: Enable WebSocket support
listener 9001
protocol websockets

# Allow anonymous access (for testing; restrict for production)
allow_anonymous true
